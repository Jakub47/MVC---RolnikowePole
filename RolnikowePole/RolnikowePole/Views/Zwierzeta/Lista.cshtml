@model IEnumerable<RolnikowePole.Models.Zwierze>
@{
    ViewBag.Title = "Lista";
}

<div class="container">
    <div class="row">
        @Html.Action("GatunkiMenu", "Zwierzeta")
        <div class="col-lg-9">

            @*<div class="col-lg-12 bg-dark m-1 p-3" id="Sortowanie">
                <div class="btn-group">
                    @Html.DropDownList("Wojewodztwa",  new SelectList(ViewBag.Wojewodztwa), "Prosze wybrac wojwodztwo" ,
                    new { @class = "form-control" })

                </div>
                <div class="btn-group">
                    <button type="button" class="btn btn-info">
                        Najnowsze
                    </button>
                </div>
                <div class="btn-group">
                    <button type="button" class="btn btn-info">
                        Najtańsze
                    </button>
                </div>

            </div>

            <div class="w-100"></div>*@

            @Html.Partial("_ZwierzetaList")
        </div>
    </div>
</div>
@section Scripts
{
    @System.Web.Optimization.Scripts.Render("~/bundles/jqueryAndjqueryUI")

    <script>
        $(document).ready(function () {
            $('#Wojewodztwa').change(function () {
                var nazwaJ = $('#Wojewodztwa Option:Selected').text();
                $.ajax({
                    type: "GET",
                    dataType: "json",
                    @*url: "@Url.Action("ZaktuazlizujWidok", "Zwierzeta")",*@
                    data: { nazwa: nazwaJ },
                    success: function (result) {
                        console.log('Powinno zmienic');
                        $('#SekcjaListaZwierzat').text('dsad');
                    }
                });
            });
        });
    </script>
}


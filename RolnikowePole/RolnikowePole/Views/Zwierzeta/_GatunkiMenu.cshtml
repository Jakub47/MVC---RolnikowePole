@model IEnumerable<RolnikowePole.Models.Gatunek>
@using RolnikowePole.Infrastucture

<div class="col-lg-3" style="padding-bottom: 30px;">
    <div class="list-group">
        @foreach (var gatunki in Model)
        {
            <a href="@Url.Action("Lista","Zwierzeta", new { nazwaGatunku = gatunki.NazwaGatunku.ToLower() })" class="list-group-item " style="text-align: center; background-color: dimgray; text-decoration: none;">
                @gatunki.NazwaGatunku
            </a>
        }
    </div>
</div>
